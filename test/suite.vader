# Should find the next line with title
Given (three different types of titles):
  ============================================================
  1. Section title                                  *TitleTag*

  ------------------------------------------------------------

  1.1. Subsection title                          *SubTitleTag*


Execute (Find lines with section's titles):
  AssertEqual 2, hp#NextSectionNum(1)
  AssertEqual 6, hp#NextSectionNum(2)

Execute(Extract section names):
  AssertEqual '1. Section title', hp#ExtractSectionName(getline(2))
  AssertEqual '1.1. Subsection title', hp#ExtractSectionName(getline(6))

Execute(Extract section tag):
  AssertEqual 'TitleTag', hp#ExtractSectionTag(getline(2))
  AssertEqual 'SubTitleTag', hp#ExtractSectionTag(getline(6))

Execute(Extract fold level):
  AssertEqual 1, hp#ExtractFoldLevel(getline(2))
  AssertEqual 2, hp#ExtractFoldLevel(getline(6))

Execute(Generate string with the content):
  let result = hp#GenerateHelpContent(45)
  AssertEqual 'CONTENT',
              \ result[0]
  AssertEqual '    1. Section title............|TitleTag|', 
              \ result[1]
  AssertEqual '        1.1. Subsection title...|SubTitleTag|',
              \ result[2]

" Execute(Insert the Content after cursor):
"   execute "normal! ggO\<esc>"
"   GenerateContent

" Expect(Content with two sections):
"   ???

